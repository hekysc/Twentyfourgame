# 工作接续清单（进行中）
本清单结合仓库 AGENTS 指南，记录未完成事项与按日期的完成日志。

## 下一步工作

## 待完成（功能点）
- 统计页进一步可视化与指标细化（可按优先级推进）
- 拖拽编辑的边界用例梳理（插入占位、撤销等）

## 完成记录
- 2025-09-20：精简提示/反馈系统，Basic 模式自动结算失败可重试，Pro 模式提示改为 Toast 与答案覆盖。
- 2025-09-21：全局自定义导航，移除系统返回箭头并补充 H5 兜底样式。

- 2025-09-27：移除静态牌面图片，改用组件渲染以缩减包体；已同期更新文档记录。
- 2025-11-15：修复设置页切换 JQK=11/12/13 后返回程序仍按 JQK=1 计算的问题，确保偏好即刻覆盖会话缓存。
- 2025-11-15：为“下一题”操作新增进行中锁并在切题前暂停计时器、发题后再启动，彻底避免跳题触发超时逻辑导致的重复失败记录。
- 2025-11-16：梳理 mp-weixin 在 Android/iOS/鸿蒙 的兼容策略，完善 system-compat 平台识别与缓存机制并补充指引文档。
- 2025-11-16：修正撤销按钮在部分端上一次点击触发多次撤销的问题，通过传递 tap 事件时间戳并在页面侧去抖，保证每次点击仅回退一步。
- 2025-11-17：修复微信小程序真机快速登录失败的问题，改为正确解析 uni.login Promise 的 [err, res] 结构，确保能拿到临时登录 code 并完成云端校验。
- 2025-11-17：修复云函数运行时报 “Cannot find module '../common/auth.js'” 的问题，统一改用 `require('common/...')` 引用公共模块以匹配 uniCloud 部署结构，重新上传 `login`/`user`/`game` 三个函数即可恢复登录。
- 2025-11-17：修复微信云函数导出未暴露 `main` 导致 `require(...).main is not a function` 的问题，统一改写 `login`/`user`/`game` 三个云函数为 `exports.main = async (...) => {}`，请重新上传后在真机复测登录。
- 2025-11-18：复现“未配置微信小程序密钥”错误并提醒：`process.env.WX_APPID/WX_SECRET` 缺失会触发该异常，需在 uniCloud 控制台为 `login` 云函数添加上述环境变量并重新上传部署。
- 2025-11-18：修复“权限校验未通过”导致无法获取微信昵称/头像的问题，将 `utils/cloud-store.js` 全量改为通过 `login/user/game` 云函数获取数据，并扩展云函数及 schema，杜绝前端匿名直接访问数据库。
